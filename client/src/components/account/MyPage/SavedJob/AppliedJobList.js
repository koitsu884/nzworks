import React from 'react'
import PropTypes from 'prop-types'

const AppliedJobList = props => {
    return (
        <div>
            
        </div>
    )
}

AppliedJobList.propTypes = {
    jobList: PropTypes.array
}

export default AppliedJobList
